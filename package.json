{
  "name": "@payperbyte/sdk",
  "version": "0.2.1",
  "type": "commonjs",
  "description": "PayPerByte TypeScript SDK \u2014 cryptographically attested, provenance-verifiable data for AI agents: proves which publisher signed the exact bytes (authenticity + tamper-evidence, not data correctness). Pay-per-call via x402 in USDC on Base mainnet; every payload carries an EIP-712 PayloadAttestation receipt you verify before acting. No token, no API keys. Subscribe to first-party on-chain streams on Arbitrum Sepolia (audit-gated).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "abis"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "lint": "eslint src/",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "viem": "^2.20.0"
  },
  "peerDependencies": {
    "@x402/core": "^2.13.0",
    "@x402/evm": "^2.13.0"
  },
  "peerDependenciesMeta": {
    "@x402/core": {
      "optional": true
    },
    "@x402/evm": {
      "optional": true
    }
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0",
    "vitest": "^2.0.0"
  },
  "keywords": [
    "byte",
    "ai-agents",
    "attested-data",
    "provenance",
    "x402",
    "usdc",
    "arbitrum",
    "base",
    "payperbyte",
    "eip-712"
  ],
  "homepage": "https://payperbyte.io",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/0rkz/ppb-sdk.git"
  },
  "bugs": {
    "url": "https://github.com/0rkz/ppb-sdk/issues"
  },
  "author": "0rkz (https://github.com/0rkz)",
  "license": "MIT"
}
